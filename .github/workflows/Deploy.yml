```yaml
name: Deploy Password Calculator to GitHub Pages

on:
  push:
    branches: [ main ]  # 监听 main 分支推送
  workflow_dispatch:    # 允许手动触发

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout
        uses: actions/checkout@v4

      # 2. 重命名 password-calculator.txt → index.html（关键步骤！）
      - name: Rename password-calculator.txt to index.html
        run: |
          if [ -f "password-calculator.txt" ]; then
            mv password-calculator.txt index.html
            echo "✅ Renamed password-calculator.txt → index.html"
          else
            echo "⚠️ password-calculator.txt not found. Using existing index.html (if any)."
          fi

      # 3. 设置 GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # 4. 上传 artifact（根目录下的 index.html）
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # 上传当前目录（含 index.html）

      # 5. 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # 6. 输出部署链接
      - name: Output deployment URL
        run: |
          echo "🚀 Deployed successfully!"
          echo "🔗 Visit: https://lonelyjaker.github.io/password-calculator/"
```